import {Text} from '@components/ui/core/text';
import {View} from '@components/ui/core/view';
import React from 'react';
import {Item} from './items';

export function LanguageItem() {
  return (
    <>
      <Item text="Language" value={'tr'} onPress={() => {}} />
    </>
  );
}
